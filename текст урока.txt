PE10.5 Генератор постов для блога в Telegram. Часть 1 (архив)
Предыдущий урок	
Есть задание
Следующий урок
Дорогой студент! Интерфейсы современных приложений могут очень быстро меняться. Если вдруг то, что видишь ты, отличается от того, что демонстрирует эксперт, настолько, что тебе непонятно, куда нажимать — напиши об этом в учебный чат, указав номер урока и видео. Наш куратор обязательно поможет тебе разобраться, а мы постараемся как можно скорее актуализировать материал :)

Уважаемые студенты!


В рамках данного урока могут рассматриваться примеры передачи персональных данных при регистрации на иностранных сервисах, а также обработки и хранения пользовательских данных.



В соответствии с требованиями Федерального закона № 152-ФЗ «О персональных данных», обращаем ваше внимание: в демонстрационных примерах и при выполнении домашних заданий необходимо использовать исключительно тестовые данные или тестовые аккаунты, в которых не указаны персональные данные — используйте вымышленные ники, дополнительные данные и т.п.



Использование иностранных сервисов не является обязательным. Вы вправе выполнять домашние задания на аналогичных разрешённых платформах по вашему выбору.

Результат дня

Создадим генератор постов для блога в Telegram.

Эксперт

Михаил Савченко:

основатель web3 проекта inite;
эксперт по блокчейн-архитектуре, децентрализации, цифровой безопасности и по защите данных в системах генеративного ИИ;
MBA в управлении IT проектами, блокчейн архитектор.
Время изучения материалов

▶️ Время чтения и просмотра:
~ 40 мин
⏱ Время выполнения задания:
~ 1 час
Инструменты урока

Google Colab (аналог Jupyter Notebook)


Облачная платформа для разработки и выполнения Python-кода в браузере.

Koyeb (аналог Render)


Облачный сервис для автоматического развёртывания приложений и серверов, предоставляющий простую платформу для запуска веб-приложений, API и фоновых задач с минимальной настройкой.

GitHub


Крупнейший веб-сервис для хостинга IT-проектов и их совместной разработки.

Zapier (аналог Make)


Инструмент для автоматизации рабочих процессов, который позволяет связывать различные веб-приложения и автоматизировать задачи, такие как отправка сообщений или обновление данных без написания кода.

Теория на сегодня


Разработка базового скрипта генерации постов в Google Colab.
Развертывание на платформе Koyeb.
Два способа автоматизации публикаций постов через Zapier.
1. Работа в Google Colab


Google Colab — это публичная установка Jupyter Notebook, тетради, в которую можно записывать скрипты на Python, делать пометки и т.д. Полезна в обучении.

✅ Jupyter Notebook — это рабочая тетрадь, в которую можно записывать как текст, так и код на Python. При этом код можно исполнять построчно.

Перед тем, как приступить к созданию основного проекта, мы создадим небольшой учебный проект в Google Colab. В этом проекте мы будем автоматизировать создание постов для социальных сетей на различные темы, используя API GPT. Это позволит освоить основные принципы работы с API и подготовить базу для более сложных задач.

1. Переходим на сайт Google Colab (https://colab.research.google.com/)



2. Заходим с помощью своего аккаунта в Google.


3. Нажимаем кнопку "Создать блокнот", чтобы создать новый проект.


Поэтапно переносим код в свой блокнот:

1. Сначала необходимо установить внешнюю библиотеку. Для решения поставленной задачи нам понадобится библиотека от компании OpenAI, отвечающая за взаимодействие с моделями OpenAI. Пишем:

!pip install openai
2. Запускаем код нажатием на кнопку “Play” рядом со строчкой кода. Библиотека устанавливается.

💡 Если в коде будет какая-то ошибка, кнопка Play станет красной и ниже программа выведет описание ошибки.

3. Для каждой части кода нужна своя кодовая ячейка. Нажимаем на “+Код”, чтобы создать новую кодовую ячейку (и повторяем это после каждого этапа написания кода).


4. Теперь необходимо подключить библиотеку OpenAI, которую только что установили, а также библиотеку OS. Она нужна для того, чтобы задать переменную окружению и ввести API-ключ.

import os
import openai
from google.colab import userdata

openai.api_key = "api_key"
Напоминаем, как получить API-ключ:
5. Создание базового скрипта генерации постов:

def generate_post(topic):
    prompt_post = f"Напишите подробный пост для блога на тему: {topic}."
    response_post = openai.chat.completions.create(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": prompt_post}],
        max_tokens=50,
        n=1,
        stop=None,
        temperature=0.7,
    )
    post_content = response_post.choices[0].message.content.strip()

    return post_content
Разберёмся, как работает этот код. Первая строка задаёт саму функцию generate_post, которая принимает на вход одну переменную (topic).



Переменная prompt задаёт промпт: "Напишите подробный пост для блога на тему {topic}".



Переменной response присваивается результат работы библиотеки OpenAI:

response = openai.chat.completions.create(

Cпецификация интерфейса библиотеки OpenAI может различаться в разных версиях. Существует как минимум три варианта интерфейса, и нейросети, генерируя код, часто используют не самые актуальные спецификации. Если в твоём коде эта строка выглядит иначе, чем в нашем примере, это означает, что ты используешь другую версию библиотеки и, возможно, потребуется внести небольшие корректировки в вызов.

В следующей строке кода указана модель нейросети: gpt-4o-mini. Затем в переменную message передаётся сам промпт и указывается количество токенов — здесь указано значение 50, поскольку пока что это базовый вариант скрипта.

Для использования модели gpt-4o-mini необходимо иметь соответствующий доступ и достаточный баланс на аккаунте OpenAI. Чтобы использовать другую модель, замените «gpt-4o-mini» на название нужной модели (например, «gpt-4o»).

Строки n=1, stop=None, temperature=0.7 в коде — это стандартные настройки вызова.



В последней строчке этого фрагмента кода указывается ответ, из которого извлекается результат.



6. Выполним этот фрагмент кода.



7. В следующем фрагменте кода указывается список тем:

topics = [
    "Преимущества медитации",
    "Здоровое питание для занятых людей",
    "Советы по управлению временем",
    "Как начать свой бизнес",
    "Путешествия по бюджету"
]
По сути, это просто массив данных. Запускаем этот код и переходим к следующему фрагменту.



8. Следующая часть кода — это код для запуска базового скрипта с форматированием текста (указанием темы, текста поста и визуальной отбивкой постов друг от друга).

for topic in topics:
    generated_post = generate_post(topic)
    print(f"Тема: {topic}\n")
    print(generated_post)
    print("---")
9. Запустим этот код. Пост указывается в усечённом формате, так как в коде прописан лимит в 50 токенов.


Рассмотрим расширенный скрипт

def generate_post(topic):
    prompt_title = f"Придумайте привлекательный заголовок для поста на тему: {topic}"
    response_title = openai.chat.completions.create(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": prompt_title}],
        max_tokens=50,
        n=1,
        temperature=0.7,
    )
    title = response_title.choices[0].message.content.strip()

    prompt_meta = f"Напишите краткое, но информативное мета-описание для поста с заголовком: {title}"
    response_meta = openai.chat.completions.create(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": prompt_meta}],
        max_tokens=100,
        n=1,
        temperature=0.7,
    )
    meta_description = response_meta.choices[0].message.content.strip()

    prompt_post = f"Напишите подробный и увлекательный пост для блога на тему: {topic}. Используйте короткие абзацы, подзаголовки, примеры и ключевые слова для лучшего восприятия и SEO-оптимизации."
    response_post = openai.chat.completions.create(
        model="gpt-4o-mini",
        messages=[{"role": "user", "content": prompt_post}],
        max_tokens=2048,
        n=1,
        temperature=0.7,
    )
    post_content = response_post.choices[0].message.content.strip()

    return {
        "title": title,
        "meta_description": meta_description,
        "post_content": post_content
    }
1. Сначала мы переопределяем функцию generate_post. Также здесь появляется переменная prompt_title с заданием придумать привлекательный заголовок для поста; также мы добавили переменную prompt_meta с запросом создать мета-описание для поста — эта часть будет использоваться для служебных нужд. Вызов здесь идентичный.



2. В переменной prompt_post мы даём задание написать подробный и увлекательный пост для блога на тему {topic}, используя при этом короткие абзацы, подзаголовки, примеры и ключевые слова для лучшего восприятия и SEO-оптимизации.

Чем подробнее мы ставим задачу, тем более адекватный результат от нейросети мы получим.

Количество токенов здесь также увеличено: для заголовка максимальное число 100, для поста — 2048 токенов.



3. Запускаем эту часть кода.



4. Следующий фрагмент кода — это запуск расширенного скрипта:

post = generate_post(topic)
print(post)
Указанная здесь переменная topic уже определена выше, и в настоящий момент в этой переменной находится последнее значение массива тем ("Путешествие по бюджету").

💡 Прежде чем запустить эту программу, убедись, что все предыдущие программы работают.


5. Запускаем эту часть кода, нажав Play.



6. Результат тут будет выдаваться написанным в одну строку, что существенно усложняет чтение. Чтобы перевести это в более удобочитаемый формат, импортируем ещё одну библиотеку Python — Pprint:

import pprint
pprint.pprint(post, compact=True)
В результате мы получаем полноценный пост с разделами и привлекательным заголовком.



Далее из наработок, полученных в этой части урока, мы соберём финальный скрипт и развернём его в облаке на серверной платформе.

📖 Ссылка на проект эксперта:

https://colab.research.google.com/drive/1uf7ffsam46MzDIBAqOHeWuvCEKQMs58P?usp=sharing



Рекомендуем создавать свой проект поэтапно в отдельном блокноте, а проект эксперта использовать для сверки результатов, если что-то не работает.



Обязательно сохраняйте свой проект в процессе работы!

2. Развертывание на платформе Koyeb


💡 Код эксперта на видео и код в тексте урока различаются. Это связано с тем, что сервис с бесплатным API для поиска новостей, News API, в рамках этого проекта работает нестабильно, и мы заменили его на более стабильный Currents API.

В этой части урока мы превратим подготовленные выше наработки в полноценный скрипт на Python и развернём в виде сервиса на платформе Koyeb.



Для этого нужно подготовить скрипт app.py, в котором находятся уже созданные функции и новые. С помощью библиотеки FastAPI мы завернули скрипт в GET-запрос.

Подготовим репозиторий на GitHub:

1. Переходим на сайт GitHub и входим в свой аккаунт.

Инструкция: как создать профиль на GitHub
2. Нажимаем на плюсик в верхней части экрана и выбираем New repository.




3. В поле Repository name вводим название репозитория, например, "bloggpt".

4. Устанавливаем флажок "Add a README file", чтобы автоматически создать файл README.md, и нажимаем кнопку Create repository.

5. Создаем файл в репозитории: Add file → Create new file.


6. Даем файлу название app.py, переносим скрипт:


Скрипт для файла app.py:

💡 Важно! В коде не нужно указывать свои ключи API, иначе GitHub не сможет опубликовать проект. Все секретные ключи должны быть скрыты и не попадать в публичный доступ. Мы добавим ключи API на этапе развертывания кода на сервере.

import os
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel
import openai
import requests

app = FastAPI()

# Получаем API ключи из переменных окружения
openai.api_key = os.getenv("OPENAI_API_KEY")  # Устанавливаем ключ OpenAI из переменной окружения
currentsapi_key = os.getenv("CURRENTS_API_KEY")  # Устанавливаем ключ Currents API из переменной окружения

# Проверяем, что оба API ключа заданы, иначе выбрасываем ошибку
if not openai.api_key or not currentsapi_key:
    raise ValueError("Переменные окружения OPENAI_API_KEY и CURRENTS_API_KEY должны быть установлены")

class Topic(BaseModel):
    topic: str  # Модель данных для получения темы в запросе

# Функция для получения последних новостей на заданную тему
def get_recent_news(topic: str):
    url = "https://api.currentsapi.services/v1/latest-news"  # URL API для получения новостей
    params = {
        "language": "en",  # Задаем язык новостей
        "keywords": topic,  # Ключевые слова для поиска новостей
        "apiKey": currentsapi_key  # Передаем API ключ
    }
    response = requests.get(url, params=params)  # Выполняем GET-запрос к API
    if response.status_code != 200:
        # Если статус код не 200, выбрасываем исключение с подробностями ошибки
        raise HTTPException(status_code=500, detail=f"Ошибка при получении данных: {response.text}")
    
    # Извлекаем новости из ответа, если они есть
    news_data = response.json().get("news", [])
    if not news_data:
        return "Свежих новостей не найдено."  # Сообщение, если новости отсутствуют
    
    # Возвращаем заголовки первых 5 новостей, разделенных переносами строк
    return "\n".join([article["title"] for article in news_data[:5]])

# Функция для генерации контента на основе темы и новостей
def generate_content(topic: str):
    recent_news = get_recent_news(topic)  # Получаем последние новости по теме

    try:
        # Генерация заголовка для статьи
        title = openai.ChatCompletion.create(
            model="gpt-4o-mini",  # Используем модель GPT-4o-mini
            messages=[{
                "role": "user", 
                "content": f"Придумайте привлекательный и точный заголовок для статьи на тему '{topic}', с учётом актуальных новостей:\n{recent_news}. Заголовок должен быть интересным и ясно передавать суть темы."
            }],
            max_tokens=60,  # Ограничиваем длину ответа
            temperature=0.5,  # Умеренная случайность
            stop=["\n"]  # Прерывание на новой строке
        ).choices[0].message.content.strip()

        # Генерация мета-описания для статьи
        meta_description = openai.ChatCompletion.create(
            model="gpt-4o-mini",
            messages=[{
                "role": "user", 
                "content": f"Напишите мета-описание для статьи с заголовком: '{title}'. Оно должно быть полным, информативным и содержать основные ключевые слова."
            }],
            max_tokens=120,  # Увеличиваем лимит токенов для полного ответа
            temperature=0.5,
            stop=["."]
        ).choices[0].message.content.strip()

        # Генерация полного контента статьи
        post_content = openai.ChatCompletion.create(
            model="gpt-4o-mini",
            messages=[{
                "role": "user", 
                "content": f"""Напишите подробную статью на тему '{topic}', используя последние новости:\n{recent_news}. 
                Статья должна быть:
                1. Информативной и логичной
                2. Содержать не менее 1500 символов
                3. Иметь четкую структуру с подзаголовками
                4. Включать анализ текущих трендов
                5. Иметь вступление, основную часть и заключение
                6. Включать примеры из актуальных новостей
                7. Каждый абзац должен быть не менее 3-4 предложений
                8. Текст должен быть легким для восприятия и содержательным"""
            }],
            max_tokens=1500,  # Лимит токенов для развернутого текста
            temperature=0.5,
            presence_penalty=0.6,  # Штраф за повторение фраз
            frequency_penalty=0.6
        ).choices[0].message.content.strip()

        # Возвращаем сгенерированный контент
        return {
            "title": title,
            "meta_description": meta_description,
            "post_content": post_content
        }
    
    except Exception as e:
        # Обрабатываем ошибки генерации
        raise HTTPException(status_code=500, detail=f"Ошибка при генерации контента: {str(e)}")

@app.post("/generate-post")
async def generate_post_api(topic: Topic):
    # Обрабатываем запрос на генерацию поста
    return generate_content(topic.topic)

@app.get("/")
async def root():
    # Корневой эндпоинт для проверки работоспособности сервиса
    return {"message": "Service is running"}

@app.get("/heartbeat")
async def heartbeat_api():
    # Эндпоинт проверки состояния сервиса
    return {"status": "OK"}

if __name__ == "__main__":
    import uvicorn
    # Запуск приложения с указанием порта
    port = int(os.getenv("PORT", 8000))
    uvicorn.run("app:app", host="0.0.0.0", port=port)
💡 Мы не можем просто взять полностью код из Google Colab и загрузить его на GitHub. Если мы перенесем код без изменений, он работать не будет. Поэтому мы перерабатываем его, чтобы адаптировать для крупного проекта.

Чтобы самостоятельно переработать код, можешь использовать ChatGPT и следующий промпт (его нужно доработать в соответствии с твоими целями):



"У меня есть код из Google Colab. запомни его: <добавь свой код>".



"У меня есть код генератора блог-постов из Google Colab (который я отправил в предыдущем сообщении). Преобразуй его в FastAPI приложение со следующими улучшениями:

1. Добавь интеграцию с Currents API для получения актуальных новостей по теме поста
2. Используй эти новости как контекст при генерации контента через OpenAI API
3. Добавь:
   - Обработку переменных окружения для API ключей
   - Обработку ошибок и исключений
   - Эндпоинты для проверки работоспособности сервиса
   - Запуск через uvicorn
   - Подробные комментарии к коду

Пожалуйста, предоставь полную версию кода без сокращений, готовую к использованию."

Вот что было изменено и добавлено в код, который мы собирали для Google Colab:

1. Использование FastAPI Создан веб-сервис с двумя основными эндпоинтами:

/generate-post для генерации контента.
/heartbeat для проверки статуса сервиса.
2. Работа с переменными окружения. API-ключи OpenAI и Currents API загружаются из переменных окружения для безопасности.

3. Добавлен класс <strong class="redactor-inline-converted">Topic</strong> для валидации входных данных.

4. Интеграция с Currents API. Получаем 5 актуальных новостей по теме для улучшения контента.

5. Генерация контента с OpenAI. Создаются три блока контента:

Заголовок.
Мета-описание.
Основной текст поста, включая актуальные новости.
6. Обработка ошибок. Используется HTTPException для информирования о возможных ошибках.

7. Асинхронность. Все запросы выполняются асинхронно для повышения производительности.

8. Запуск через Uvicorn. Приложение запускается с помощью Uvicorn, с настройкой порта через переменную окружения.

9. Параметры запросов OpenAI. Оптимизированы параметры запросов, например, увеличено количество токенов для больших текстов.



Итог: Приложение теперь генерирует контент по заданной теме, учитывая актуальные новости, и готово к развертыванию на сервере.

Инструкция: как получить Currents API
Это основные изменения! Теперь код может использоваться не только для генерации постов, но и как веб-сервис, с которым можно взаимодействовать через HTTP-запросы.



7. Нажимаем Commit changes….


8. Создаем файл requirements.txt. В нем перечислены пакеты, которые потребуются для запуска скрипта:

fastapi
uvicorn
pydantic
openai==0.27.0
requests
httpx
tiktoken
Опубликуем скрипт на Koyeb:

1. Переходим на Koyeb. Нажимаем на кнопку GET STARTED.

2. Регистрируемся с помощью своего аккаунта в GitHub.


3. В левом верхнем углу нажимаем на кнопку Create Service.


4. В центре экрана нажимаем на кнопку Web service. Выбираем GitHub. Нажимаем на кнопку Next.


5. Привязываем свой аккаунт GitHub.

6. Выбираем нужный репозиторий.


7. Выбираем Free. Нажимаем на кнопку Next.


8. Раскрываем список Builder и включаем переключатель Override. В поле Run command вводим python app.py.


9. Раскрываем список Environment variables. Добавляем:

OPENAI_API_KEY
NEWSAPI_KEY

10. В правом верхнем углу нажимаем кнопку Deploy запуска.


11. Дожидаемся завершения развертывания.

12. Адрес запроса можно скопировать, ссылка пригодится нам позже.



Если вашу учетную запись заблокировали, то воспользуйтесь аналогом:


2.1 Альтернативный вариант, если сервис Koyeb работает некорректно

Сервис Koyeb имеет множество аналогов, таких как Heroku, Vercel, Google Cloud Run, Render и другие. Для развертывания приложений мы воспользуемся одной из этих платформ.

💡 Render — это платформа для хостинга приложений и облачных сервисов, которая позволяет разработчикам развёртывать веб-приложения, базы данных, статические сайты, API и фоновые задачи в облаке.

Регистрация в Render

1. Переходим на Render.com и нажимаем Get Started for Free.

2. Регистрируемся, используя свой аккаунт GitHub.


3. Нажимаем Authorize Render → Create an account.

4. На твой e-mail придёт письмо для верификации. Открываем почту, находим письмо от Render и переходим по кликабельной ссылке.

5. Заполняем информацию о себе.



Готово!

Создание веб-сервиса

1. Нажимаем +New → Web Service.


2. Подключаем GitHub для развертывания приложений из существующих репозиториев.


3. Нажимаем Install → вводим пароль от GitHub → Confirm.

4. Выбираем репозиторий, который мы подготовили для проекта → Connect.


4. В настройках:

Выбираем Free.

В Start Command пишем: uvicorn app:app --host 0.0.0.0 --port 8000

Эта команда запускает Uvicorn с вашим приложением, которое находится в модуле app.py, и выставляет его на порт 8000 с доступом на всех интерфейсах (0.0.0.0).


В Environment Variables заполняем скрытые переменные и их значения:

OPENAI_API_KEY
CURRENTS_API_KEY

6. Нажимаем Deploy Web Service и ждем завершения развертывания.

7. Проверяем, что всё работает правильно — должна появиться зелёная плашка с галочкой.

8. Копируем ссылку.


Именно с этой ссылкой мы будем работать в Zapier:


3. Автоматизации публикаций постов через Zapier


Приступим к работе с сервисом: переходим на платформу Zapier и регистрируемся, используя аккаунт Google.

💡 После регистрации предоставляется 14 дней для тестирования платных функций Zapier. По истечении этого срока необходимо перейти на платный тариф или создать новый аккаунт.

Для начала подготовим все необходимое:

Создаем бота в Телеграм:

1. Переходим в Телеграм.

2. Находим @BotFather.


3. Нажимаем Start.

4. Отправляем команду /newbot.

5. Пишем имя бота. Пишем юзернейм — он должен заканчиваться на _bot или bot.

6. Когда бот будет создан, копируем HTTP API.

7. Переходим в Zapier. Этот ключ мы будем вставлять на панели справа для действия Send Message in Telegram. Далее мы добавим бота в тестовую группу, выдадим права администратора, и бот сможет присылать сообщения в группу.

Создаем группу, где будут публиковаться посты:

1. Нажимаем на значок:


2. Выбираем “New Group”.


3. Добавляем знакомых или пропускаем этот шаг.

4. Даем группе название.

5. Добавляем бота в группу и делаем его администратором.







Создаем Zap

1. Создаем новый проект, нажав на кнопку Create и выбрав пункт Zaps.


2. Перед нами открывается поле на котором мы можем строить схему. 


3. Нажимаем на Trigger, выбираем нужное событие в поиске для добавления в схему.


Мы настроим автоматическую публикацию постов двумя способами.

Первый способ

Создадим такую схему:


Первый шаг — расписание для запа.



1. Выбираем Schedule by Zapier.

2. Определяем событие Trigger event → Every Day.


3. Настраиваем необходимые параметры.


4. Тестируем работу триггера.


Второй шаг — вызов сервиса, развернутого в Koyeb.



1. Выбираем Webhooks by Zapier.

2. Определяем Action event → POST.


3. Настраиваем: добавляем URL, полученный при работе с Koyeb. К URL также нужно добавить /generate-post.


4. Выбираем тип данных: JSON; указываем переменную topic и задаем для нее текстовое значение.


Если тест выдает ошибку:


Скорее всего, ваше приложение работает медленно или не отвечает в течение времени, которое ожидает Zapier. Чтобы исправить эту ошибку, попробуйте уменьшить максимальное количество токенов в коде:


6. Обращаемся к схеме запа, чтобы добавить второе действие. Для этого нажимаем на плюсик (add a step):


7. В новом окне в строке поиска вводим Telegram.

8. В Action event выбираем Send Message.

9. Добавляем бота с помощью API: выбираем Select → Connect a new account → вводим значение API → Yes, Continue to Telegram.

10. В поле Chat id выбираем название чата, в котором бот может публиковать.


11. В поле Text Format выбираем Markdown, потому что генеративные нейросети на базе больших языковых моделей, в том числе ChatGPT, делают разметку текста с помощью языка разметок Markdown.

12. Нажимаем на поле Message Text, в появившемся списке выбираем POST и добавляем все части поста (Title, Meta Description, Post Content). Нажимаем Continue.


13. Переходим на вкладку Test, нажимаем на кнопку Test step.

При запуске программа выдаёт ошибку, указывая на то, что в тексте присутствуют специальные символы, не поддерживаемые сервисом Telegram. Они должны быть заэкранированы. Поэтому займёмся экранированием.


✍🏻 Экранирование символов в контексте Telegram — это процесс добавления специальных символов перед определёнными знаками, чтобы они не воспринимались как часть форматирования текста. Экранирование необходимо для того, чтобы эти символы отображались в тексте сообщения как есть, а не интерпретировались как команды форматирования.

14. Для этого перед шагом Send Message in Telegram добавляем ещё один элемент — нажимаем на плюс и в поисковом поле ищем Formatter. Щёлкаем по результату поиска.



15. В настройках в поле Event выбираем Text и нажимаем Continue.


16. В следующем окне настроек в поле Transform выбираем Replace, в поле Input выбираем POST и добавляем все части поста (Title, Meta Description, Post Content).


17. В поле Find пишем #.



18. В поле Replace пишем \\#, нажимаем Continue.



19. Переходим на вкладку Test, нажимаем на кнопку Test step.

20. Перед шагом Send Message in Telegram добавляем ещё один элемент — нажимаем на плюс и в поисковом поле ищем Formatter. Щёлкаем по результату поиска.



21. В настройках в поле Event выбираем Text и нажимаем Continue.



22.В следующем окне настроек в поле Transform выбираем Replace, в поле Input выбираем третий шаг Text in Formatter by Zapier. Под шагом выбираем Output.

<p>
			</p>

💡 Мы берем результат запроса с первым экранированием символа #, где он уже был изменен на \\#, и теперь экранируем следующий символ.

23. В поле Find пишем . (точку).



24. В поле Replace пишем \\. (обратный слеш и точка), нажимаем Continue.



25. Переходим на вкладку Test, нажимаем на кнопку Test step.



26. В шаге Send Message in Telegram в поле Message Text, удаляем всё и заменяем выбранный элемент на предыдущий. Output: …


💡 Чтобы связка работала правильно, пройдите по всем запам и протестируйте каждый из них.


Повторяем эти шаги для всех символов, которые могут встретиться в тексте:



_ — \\_;

* — \\*;

! — \\! и так далее.

💡Если в опубликованном посте остаются лишние символы, то ты можешь сделать это следующим образом:

- Доработать код, указав в промпте, что символы, например, #, не нужно добавлять в текст поста.

- В Zapier Formatter вместо символов \\# в поле Replace оставить пустое место.


Второй способ

Создаём такую же схему, но вместо Webhooks by Zapier используем встроенный ChatGpt.



1. В аккаунте Zapier в правом верхнем углу нажимаем на кнопку Create. Выбираем New Zap. В открывшемся окне нажимаем Trigger — находим Schedule by Zapier. В настройках запа (панель справа) в поле Event выбираем Every day, выбираем время выхода поста — 12:00 AM, в поле Trigger on weekends выбираем no и нажимаем Continue.



2. Если в схеме у какого-то действия стоит восклицательный знак, то на панели настроек переходим во вкладку Test и нажимаем на кнопку Test.



3. Нажимаем на Action. В поисковой строке пишем chatgpt.


4. В поле Event выбираем Conversations. Переходим на вкладку Account. Выбираем свой аккаунт или создаём новый с помощью ввода API-ключа. 


5. Переходим на вкладку “Configure”. В поле User Message пишем Напиши пост на тему “Преимущества медитации” используй короткие абзацы или заголовки. В поле Model выбираем gpt-4o. Переходим на вкладку Test. Нажимаем на кнопку Test step.


6. Остальная часть схемы настраивается так же, как в предыдущем способе.

💡 Данный способ намного проще, но он дает нам меньше контроля. В коде мы можем настроить логику и обработку данных более гибко, например, использовать циклы, условия или создавать сложные запросы к API, а в интеграции ChatGPT в Zapier мы так сделать не можем.

Основные выводы урока

создали и улучшили скрипт для генерации постов для блога;
развернули скрипт на платформе Koyeb;
настроили автоматизацию публикаций в Telegram с помощью Zapier.
💡 Важно! Не забудь сохранить свой проект в Google Colab из этого урока, так как мы продолжим работать с ним в следующем уроке.

Домашнее задание

Выполни два способа автоматизации постов в Telegram с помощью Zapier:
Первый способ: с использованием кода, развернутого в Koyeb (или аналогичном сервисе).
Второй способ: с использованием встроенного в Zapier GPT.
В качестве домашнего задания приложи скриншоты:
проекта на GitHub.
схем из Zapier;
итоговых постов из группы в Telegram.
Материалы к уроку

Проект эксперта — https://colab.research.google.com/drive/1uf7ffsam46MzDIBAqOHeWuvCEKQMs58P?usp=sharing



https://github.com/ZerocoderUniversity/AI-course-prompt-engineering

